#include <bits/stdc++.h>
using namespace std;
int n;
int main(void) {
  cin >> n;
  cout << (n + 1) * n / 2 << '\n';
  cout << (int)pow((n + 1) * n / 2, 2) << '\n';
  cout << (int)pow((n + 1) * n / 2, 2) << '\n';
}
